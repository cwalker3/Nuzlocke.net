<%= turbo_frame_tag 'area' do %>
  <%= form_with model: @area, data: { turbo_frame: 'game-dashboard' } do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.hidden_field :game_id, value: @game.id %>

    <%= f.submit %>
  <% end %>
<% end %>